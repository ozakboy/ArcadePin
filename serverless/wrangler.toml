name = "arcadepin-proxy"
main = "arcadepin-proxy.js"
compatibility_date = "2024-11-01"

# Non-secret vars (edit to match your repo). Set GH_TOKEN as a SECRET, not here:
#   wrangler secret put GH_TOKEN
[vars]
GH_OWNER = "ozakboy"
GH_REPO = "ArcadePin"
EVENT_TYPE = "arcadepin_score"
ALLOWED_ORIGIN = "https://ozakboy.github.io"
