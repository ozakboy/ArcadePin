Put your app icon here.

Required file (exact name + path):
  icons/icon-512.png

- Save your highest-resolution SQUARE version of the icon as this file.
- A single 512x512 (or larger) PNG is enough — index.html and manifest.json
  already reference it for the browser tab favicon, the iOS home-screen icon,
  and the PWA install icon. Browsers downscale it automatically.

把 App 圖示放在這裡：檔名與路徑必須是 icons/icon-512.png
（存成最高解析度的正方形 PNG 即可，瀏覽器會自動縮小。）
